# GYPSI frontend environment
# Vite exposes only VITE_-prefixed vars to the browser via import.meta.env.

# Base URL of the GYPSI FastAPI backend.
# When unset, the app falls back to http://localhost:8000
VITE_API_URL=https://gypsi-trading-agent.vercel.app/
