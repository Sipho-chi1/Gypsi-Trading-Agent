FROM python:3.12-slim
WORKDIR /app

# TODO: replace with the actual Alpaca MCP server install steps, e.g.
#   RUN pip install alpaca-mcp-server
#   CMD ["alpaca-mcp-server", "--host", "0.0.0.0", "--port", "8080"]
# Verify the current package/command name against Alpaca's docs before Day 1 —
# MCP server distribution details can change.

EXPOSE 8080
CMD ["python", "-c", "print('placeholder — wire up the real Alpaca MCP server here')"]
