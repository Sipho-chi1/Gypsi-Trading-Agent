@'
[project]
name = "gypsi-trading-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
]

[tool.vercel]
entrypoint = "backend.app.main:app"
'@ | Set-Content -Path pyproject.toml -Encoding ascii