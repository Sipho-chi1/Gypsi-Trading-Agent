[project]
name = "gypsi-trading-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy[asyncio]",
    "asyncpg",
    "pydantic>=2",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "backend.app.main:app"
